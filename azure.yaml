# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: aca-vnet-demo
metadata:
  template: aca-vnet-demo@0.0.1-beta

services:
  web:
    project: ./app
    language: docker
    host: containerapp

infra:
  provider: bicep
  path: ./bicep
  module: main
